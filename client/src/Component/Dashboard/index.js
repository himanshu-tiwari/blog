import React from 'react';
import './index.scss';

const Dashboard = (props) => {
    return(
        <div className="dashboard">
            Dashboard
        </div>
    );
};

export default Dashboard;